{
  "name": "client-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --mode development --watch",
    "build": "vue-tsc -b && vite build",
    "build:dev": "vite build --mode development",
    "build:prod": "vue-tsc --noEmit  && vite build",
    "dotnet:run": "dotnet watch run --launch-profile \"http\""
  },
  "dependencies": {
    "globby": "^14.0.2",
    "vue": "^3.4.37"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "@vitejs/plugin-vue": "^5.1.2",
    "typescript": "^5.5.3",
    "vite": "^5.4.1",
    "vue-tsc": "^2.0.29"
  }
}